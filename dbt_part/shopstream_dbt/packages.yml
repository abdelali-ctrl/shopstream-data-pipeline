packages:
  # Surrogate keys, type helpers, generic tests, etc.
  - package: dbt-labs/dbt_utils
    version: 1.1.1

  # Richer test library: value ranges, row counts, column statistics.
  # Used on fact_orders and mart measures to catch numeric drift.
  - package: metaplane/dbt_expectations
    version: 0.10.3
